{{- if .Values.persistence.jwt-keys.enabled -}}
{{- if not .Values.persistence.jwt-keys.existingClaim -}}
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: {{ template "api360.fullname" . }}-jwt-keys
  labels:
    app.kubernetes.io/name: {{ include "api360.name" . }}
    helm.sh/chart: {{ include "api360.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/component: app
{{- if .Values.persistence.jwt-keys.annotations }}
  annotations:
{{ toYaml .Values.persistence.jwt-keys.annotations | indent 4 }}
{{- end }}
spec:
  accessModes:
    - {{ .Values.persistence.jwt-keys.accessMode | quote }}
  resources:
    requests:
      storage: {{ .Values.persistence.jwt-keys.size | quote }}
{{- if .Values.persistence.jwt-keys.storageClass }}
{{- if (eq "-" .Values.persistence.jwt-keys.storageClass) }}
  storageClassName: ""
{{- else }}
  storageClassName: "{{ .Values.persistence.jwt-keys.storageClass }}"
{{- end }}
{{- end }}
{{- end -}}
{{- end -}}